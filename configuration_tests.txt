# Tests pour la classe configuration

isValid()
1/ Creer config avec des pieces sans dependances 
config.isValid doit renvoyer vrai

2/ Creer config avec des pieces avec une requirement corrects
config.isValid doitr renvoyer vrai

3/ Creer config avec des pieces avec un incompatibilite
config.isValid doit renvoyer faux

----------------------------------------

isComplete()
1/Ajouter une part pour chaque categorie
isComplete doit renvoyer true

2/Ajouter une part pour chaque categorie sauf une
isComplete doit renvoyer false

2/Ajouter une part pour chaque categorie sauf deux
isComplete doit renvoyer false

----------------------------------------

selectPart(PartType chosenPart)
getSelectedParts()
1/Ajouter UNE part a la config
le set renvoyé doit contenir cette part
getSelectedParts().contains(part)

2/Ajouter DES partS a la config
le set renvoyé doit contenir ces parts
getSelectedParts().containsAll(listPart)

----------------------------------------

getSelectionForCategory(Category category)
1/ Ajouter une part d'une categorie spécifique
LA part retournée doit être cette derniere
quand on demande la même catégorie
getSelectionForCategory(sameCategory).equals(ourPart)

----------------------------------------

unselectPartType(Category categoryToClear)
1/ Ajouter une part a une categorie spécifique
2/ Enlever une part de la meme categorie
3/ getSelectionForCategory(sameCategory)
Verifier que l'objet retourné est null

----------------------------------------

clear()
1/ Ajouter des parts a une configuration
2/ Clear une configuration
3/ Verifier que chaque part est nulle




